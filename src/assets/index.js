const FolderAsset = require('./FolderAsset')
const ImageAsset = require('./ImageAsset')
const StringAsset = require('./StringAsset')

module.exports = {
    FolderAsset,
    ImageAsset,
    StringAsset,
}
