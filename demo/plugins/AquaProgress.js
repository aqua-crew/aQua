class AquaProgress {
    install(aqua) {
        const lifetimes = aqua.lifetimes

        lifetimes.on('setup', () => {

        })

        lifetimes.on('ready', () => {

        })
    }
}

module.exports = AquaProgress
